name: build deep learning windows

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build_windows:
    runs-on: windows-latest
    strategy:
      matrix:
        configuration: [Debug, Release]
        platform: [x64]
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: 'true'
      - uses: microsoft/setup-msbuild@v2
        with:
          msbuild-architecture: x64
      - shell: cmd
        run: |
          msbuild ./build/Deep-Learning.sln /p:Configuration=${{matrix.configuration}} /p:Platform=${{matrix.platform}}
